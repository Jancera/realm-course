import { ObjectSchema } from "realm";

const TaskSchema: ObjectSchema = {
  name: "Task",
  properties: {
    _id: "int",
    name: "string",
    status: "string?",
  },
  primaryKey: "_id",
};

export default TaskSchema;
